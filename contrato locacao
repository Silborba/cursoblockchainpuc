pragma solidity 0.5.12;

    contract prestacaoDeServicos {
 
    string public prestador;
 
    string public tomador;
    uint256 private horasTrabalhadas;
    uint256 constant valorhora = 300;
    uint256 private remuneracaofinal;
    
     constructor( string memory _prestador, 
                  string memory _tomador, 
                  uint256 _valorhora,
                  uint256 _horasTrabalhadas) public {
                      
        prestador = _prestador;
        tomador = _tomador;
        valorhora = _valorhora;
        horasTrabalhadas = _horasTrabalhadas;
        
        if (valorhora > 300)
        {
        valorhora = 300;
        } else if  (_valorhora>200 && _horasTrabalhadas>220 = );
            
        
     }
        else if (_valorhora>200 && _valorhora<300){
         valor = 250
     } 
     else if (_valor ==197) {
         valor = 1;
     }
         
     }
     valor =
        function calculoRemuneracao () (uint256 valorhora, uint256 horas)  public view returns (uint256 remuneracaofinal)
        {
        return ;
        // função só serve para duas coisas. ou vai alterar o contrato ou vai retornar alguma coisa
    }
    function simulaMulta( uint256 mesesRestantes, uint256 totalMesesContrato)
    public
    view
    returns(uint256 valorMulta) {
        
        valorMulta = valor*multaMaximoAluguelRescisao;
        valorMulta = valorMulta/totalMesesContrato;
        valorMulta = valorMulta*mesesRestantes;
        
        return valorMulta;
